# Airflow Init image - runs migrations and creates admin user
# Managed by Ansible

FROM airflow-base:latest

USER airflow

COPY --chown=airflow:root docker/init/entrypoint.sh /entrypoint.sh

ENTRYPOINT ["/entrypoint.sh"]
